import { Request, Response } from 'express';

export const orderController = {
  getAll: (_req: Request, _res: Response): void => {
    // TODO: respond with all orders
  },

  deleteById: (_req: Request, _res: Response): void => {
    // TODO: delete order by id, respond with updated list
  },
};
